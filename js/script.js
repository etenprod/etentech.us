  document.addEventListener('DOMContentLoaded', () => {

  const projectCases = {
  "probouw": {
    "accent": "#d91f26",
    "title": "Probouw Store",
    "category": "E-commerce catalogue",
    "intro": "We structured a tile and materials catalogue so customers can find collections faster, understand the range, and move toward a quote request.",
    "scope": "Catalogue - Filters - Quote flow",
    "result": "+42%",
    "overview": "The project needed a clear store logic: homepage, categories, collections, product blocks, and a contact path without extra friction. We connected visual material browsing with a simple enquiry flow.",
    "challenges": [
      "Show a large catalogue without visual overload.",
      "Make the quote request path easy to notice.",
      "Keep the catalogue clean on mobile screens."
    ],
    "solutions": [
      "Built a clear hierarchy for categories and collections.",
      "Placed key actions close to product blocks.",
      "Adapted cards, filters, and previews for fast browsing."
    ],
    "results": [
      "Faster collection browsing",
      "Clearer catalogue structure",
      "Fewer steps to enquiry"
    ],
    "images": [
      "images/sites/probouw/1920w light.png",
      "images/sites/probouw/600-380w light.png",
      "images/sites/probouw/600-380.png"
    ],
    "gallery": [
      {
        "type": "scene-split",
        "images": [
          "images/sites/probouw/1920w light-1.png",
          "images/sites/probouw/600-380w light.png"
        ]
      },
      {
        "type": "scene-angle",
        "images": [
          "images/sites/probouw/600-380.png",
          "images/sites/probouw/1920w light.png",
          "images/sites/probouw/600-380w light.png"
        ]
      },
      {
        "type": "scene-stack",
        "images": [
          "images/sites/probouw/1920w light.png",
          "images/sites/probouw/600-380w light.png"
        ]
      },
      {
        "type": "scene-mobile",
        "images": [
          "images/sites/probouw/600-380.png",
          "images/sites/probouw/600-380w light.png"
        ]
      }
    ]
  },
  "kolyba": {
    "accent": "#c70000",
    "title": "Kolyba Restaurant",
    "category": "Restaurant website",
    "intro": "An atmospheric restaurant website where menu, events, delivery, and booking are brought into one warm and expressive experience.",
    "scope": "Website - Menu - Booking",
    "result": "+58%",
    "overview": "The main goal was to express the restaurant character without losing practical clarity. The page had to create mood, present offers, and guide guests toward booking.",
    "challenges": [
      "Balance restaurant atmosphere with functionality.",
      "Make menu and events easy to browse.",
      "Keep booking visible inside rich content."
    ],
    "solutions": [
      "Built the pages around a strong first screen.",
      "Added separate paths for menu, events, and delivery.",
      "Reinforced booking calls in key sections."
    ],
    "results": [
      "More table bookings",
      "Clearer menu structure",
      "Stronger brand atmosphere"
    ],
    "images": [
      "images/sites/kolyba/600-380w-1.png",
      "images/sites/kolyba/Site Kolyba_Menu.png",
      "images/sites/kolyba/600-380w.png"
    ],
    "gallery": [
      {
        "type": "scene-split",
        "images": [
          "images/sites/kolyba/1920w.png",
          "images/sites/kolyba/Site Kolyba_Menu.png"
        ]
      },
      {
        "type": "scene-angle",
        "images": [
          "images/sites/kolyba/600-380w-1.png",
          "images/sites/kolyba/600-380w.png",
          "images/sites/kolyba/Site Kolyba_Menu.png"
        ]
      },
      {
        "type": "scene-stack",
        "images": [
          "images/sites/kolyba/Site Kolyba_Menu.png",
          "images/sites/kolyba/600-380w.png"
        ]
      },
      {
        "type": "scene-mobile",
        "images": [
          "images/sites/kolyba/600-380w-1.png",
          "images/sites/kolyba/600-380w.png"
        ]
      }
    ]
  },
  "monex": {
    "accent": "#c89a57",
    "title": "Monex Exchange",
    "category": "Fintech platform",
    "intro": "An exchange interface for cash and crypto with live rates, calculator, request flow, offices, and FAQ in one clear path.",
    "scope": "UI - Calculator - Requests",
    "result": "+36%",
    "overview": "For a finance service, the interface needs to reduce hesitation quickly: show rates, explain the action, and provide a simple request form. We built a dark UI focused on fast calculation and trust.",
    "challenges": [
      "Show a lot of data without making it feel complex.",
      "Make the calculator the central interaction.",
      "Support trust through structure and FAQ."
    ],
    "solutions": [
      "Placed rates and exchange flow upfront.",
      "Shortened the path from calculation to request.",
      "Added clear office, terms, and FAQ blocks."
    ],
    "results": [
      "Faster request path",
      "Clear exchange scenario",
      "More trust in the service"
    ],
    "images": [
      "images/sites/monex/[LIGHT] Landing Page (WIDE).png",
      "images/sites/monex/[LIGHT] Landing Page (768-300).png"
    ],
    "gallery": [
      {
        "type": "scene-split",
        "images": [
          "images/sites/monex/[LIGHT] Landing Page (WIDE).png",
          "images/sites/monex/[LIGHT] Landing Page (768-300).png"
        ]
      },
      {
        "type": "scene-angle",
        "images": [
          "images/sites/monex/[LIGHT] Landing Page (768-300).png",
          "images/sites/monex/[LIGHT] Landing Page (WIDE).png"
        ]
      },
      {
        "type": "scene-stack",
        "images": [
          "images/sites/monex/[LIGHT] Landing Page (WIDE).png",
          "images/sites/monex/[LIGHT] Landing Page (768-300).png"
        ]
      }
    ]
  },
  "oknaniko": {
    "accent": "#3572ff",
    "title": "OknaNiko",
    "category": "Service landing",
    "intro": "A landing page for window repair that quickly explains the problem, builds trust, and guides users toward contact.",
    "scope": "Landing - Trust - Requests",
    "result": "+64%",
    "overview": "The page needed to work for a quick decision: users recognize the problem, see the service, check trust signals, and contact the business. We shaped that into one direct flow with a strong hero.",
    "challenges": [
      "Explain the service problem immediately.",
      "Show benefits without long reading.",
      "Make mobile just as convenient."
    ],
    "solutions": [
      "Built the hero around the customer pain point.",
      "Added benefits, process, and review sections.",
      "Placed contact actions in highly visible areas."
    ],
    "results": [
      "More client requests",
      "Shorter path to contact",
      "Better mobile experience"
    ],
    "images": [
      "images/sites/oknaniko/OknaNiko Desktop 1920.png",
      "images/sites/oknaniko/OknaNiko Mobile 375.png"
    ],
    "gallery": [
      {
        "type": "scene-split",
        "images": [
          "images/sites/oknaniko/OknaNiko Desktop 1920.png",
          "images/sites/oknaniko/OknaNiko Mobile 375.png"
        ]
      },
      {
        "type": "scene-angle",
        "images": [
          "images/sites/oknaniko/OknaNiko Desktop 1920.png",
          "images/sites/oknaniko/OknaNiko Mobile 375.png"
        ]
      },
      {
        "type": "scene-mobile",
        "images": [
          "images/sites/oknaniko/OknaNiko Mobile 375.png",
          "images/sites/oknaniko/OknaNiko Desktop 1920.png"
        ]
      }
    ]
  }
};

  const initSmoothScroll = () => {
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', (e) => {
        const targetId = anchor.getAttribute('href');

        if (!targetId || targetId === '#') return;

        const targetElement = document.querySelector(targetId);

        if (targetElement) {
          e.preventDefault();
          targetElement.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });
  };

  const initAccordion = () => {
    const faqItems = document.querySelectorAll('.faqItem');

    faqItems.forEach(item => {
      const btn = item.querySelector('.faqBtn');
      if (!btn) return;

      btn.addEventListener('click', () => {
        const isOpen = item.getAttribute('data-open') === 'true';
        item.setAttribute('data-open', String(!isOpen));
        btn.setAttribute('aria-expanded', String(!isOpen));
      });
    });
  };

  const initRevealBlocks = () => {
    const blocks = document.querySelectorAll('#services, .consultBlock, #pricing, #faq, .startProject');

    if (!blocks.length) return;

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.16 });

    blocks.forEach(block => {
      block.classList.add('revealBlock');
      observer.observe(block);
    });
  };

  const initMobileMenu = () => {
    const header = document.querySelector('.header');
    const burger = document.querySelector('.header__burger');
    const drawer = document.querySelector('.header__drawer');

    if (!burger || !header) return;

    const toggleMenu = (forceClose = false) => {
      let isNowOpen;

      if (forceClose) {
        header.classList.remove('is-open');
        isNowOpen = false;
      } else {
        isNowOpen = header.classList.toggle('is-open');
      }

      burger.setAttribute('aria-expanded', String(isNowOpen));
      if (drawer) drawer.setAttribute('aria-hidden', String(!isNowOpen));
    };

    burger.addEventListener('click', () => toggleMenu());

    header.querySelectorAll('.header__drawer a').forEach(link => {
      link.addEventListener('click', () => toggleMenu(true));
    });
  };

  const initFeaturedProjects = () => {
    const section = document.querySelector('.featuredProjects');
    const stage = document.querySelector('[data-featured-projects]');

    if (!section || !stage) return;

    const projects = [
      {
        slug: 'probouw',
        accent: '#d91f26',
        accentSoft: '#f1c27d',
        metric: '+42%',
        image: 'images/sites/previews/probouw.webp',
        metricLabel: 'Catalogue Usability',
        category: 'E-commerce catalogue',
        title: 'Probouw Store',
        service: 'CATALOGUE - FILTERS',
        description: 'A clear path from collections to quote requests'
      },
      {
        slug: 'kolyba',
        accent: '#c70000',
        accentSoft: '#f1c27d',
        metric: '+58%',
        image: 'images/sites/previews/kolyba.webp',
        metricLabel: 'Table Bookings',
        category: 'Restaurant website',
        title: 'Kolyba Restaurant',
        service: 'WEBSITE - MENU',
        description: 'A warm restaurant story with booking flow'
      },
      {
        slug: 'monex',
        accent: '#c89a57',
        accentSoft: '#e01818',
        metric: '+36%',
        image: 'images/sites/previews/monex.webp',
        metricLabel: 'Exchange Requests',
        category: 'Fintech platform',
        title: 'Monex Exchange',
        service: 'UI - EXCHANGE FLOW',
        description: 'A dark fintech interface for exchange requests'
      },
      {
        slug: 'oknaniko',
        accent: '#3572ff',
        accentSoft: '#ff8a3d',
        metric: '+64%',
        image: 'images/sites/previews/oknaniko.webp',
        metricLabel: 'Client Requests',
        category: 'Service landing',
        title: 'OknaNiko',
        service: 'LANDING - TRUST',
        description: 'A conversion-focused page for fast client calls'
      }
    ];

    const fields = {
      hero: stage.querySelector('[data-project-hero]'),
      copy: stage.querySelector('[data-project-copy]'),
      metric: stage.querySelector('[data-project-metric]'),
      metricLabel: stage.querySelector('[data-project-metric-label]'),
      category: stage.querySelector('[data-project-category]'),
      title: stage.querySelector('[data-project-title]'),
      service: stage.querySelector('[data-project-service]'),
      description: stage.querySelector('[data-project-description]'),
      image: stage.querySelector('[data-project-image]')
    };

    let activeIndex = 0;

    const renderProject = (index) => {
      const project = projects[index];

      stage.style.setProperty('--project-accent', project.accent);
      stage.style.setProperty('--project-accent-soft', project.accentSoft);

      Object.keys(fields).forEach(key => {
        if (!fields[key]) return;

        if (key === 'metric') {
          fields[key].textContent = project.metric;
          return;
        }

        if (key === 'image') {
          fields[key].src = project.image;
          fields[key].alt = project.title;
          return;
        }

        fields[key].textContent = project[key] || '';
      });
    };

    const changeProject = (direction) => {
      activeIndex = (activeIndex + direction + projects.length) % projects.length;
      stage.classList.remove('is-changing');
      void stage.offsetWidth;
      stage.classList.add('is-changing');
      renderProject(activeIndex);
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          section.classList.add('is-visible');
          observer.unobserve(section);
        }
      });
    }, { threshold: 0.28 });

    const prevButton = section.querySelector('[data-project-prev]');
    const nextButton = section.querySelector('[data-project-next]');

    if (prevButton) prevButton.addEventListener('click', () => changeProject(-1));
    if (nextButton) nextButton.addEventListener('click', () => changeProject(1));

    renderProject(activeIndex);
    observer.observe(section);
  };

  const initCustomSelects = () => {
    const selects = document.querySelectorAll('[data-custom-select]');

    if (!selects.length) return;

    const closeAll = (except = null) => {
      selects.forEach(select => {
        if (select !== except) {
          select.classList.remove('is-open');
          select.querySelector('[data-custom-select-button]')?.setAttribute('aria-expanded', 'false');
        }
      });
    };

    const syncButtonText = (select) => {
      const nativeSelect = select.parentElement?.querySelector('.startProject__nativeSelect');
      const trigger = select.querySelector('[data-custom-select-button]');
      const activeOption = select.querySelector(`button[data-value="${nativeSelect?.value || ''}"]`);

      if (trigger && activeOption) {
        trigger.textContent = activeOption.textContent;
      } else if (trigger && nativeSelect && !nativeSelect.value) {
        trigger.textContent = 'Select...';
      }
    };

    selects.forEach(select => {
      const trigger = select.querySelector('[data-custom-select-button]');
      const menu = select.querySelector('[data-custom-select-menu]');
      const nativeSelect = select.parentElement?.querySelector('.startProject__nativeSelect');

      if (!trigger || !menu || !nativeSelect) return;

      trigger.addEventListener('click', () => {
        const isOpen = select.classList.toggle('is-open');
        closeAll(select);
        trigger.setAttribute('aria-expanded', String(isOpen));
      });

      menu.querySelectorAll('button[data-value]').forEach(option => {
        option.addEventListener('click', () => {
          nativeSelect.value = option.dataset.value || '';
          menu.querySelectorAll('button').forEach(item => item.classList.remove('is-active'));
          option.classList.add('is-active');
          syncButtonText(select);
          select.classList.remove('is-open');
          trigger.setAttribute('aria-expanded', 'false');
        });
      });
    });

    document.addEventListener('click', (event) => {
      if (!event.target.closest('[data-custom-select]')) {
        closeAll();
      }
    });

  };

  const initStartProjectForm = () => {
    const form = document.querySelector('.startProject__form');
    const status = document.querySelector('[data-form-status]');
    const params = new URLSearchParams(window.location.search);

    if (status && params.get('sent') === '1') {
      status.classList.add('is-visible');
    }

    if (!form || window.location.protocol !== 'file:') return;

    form.addEventListener('submit', (event) => {
      event.preventDefault();

      const formData = new FormData(form);
      const help = formData.getAll('help').join(', ') || '-';
      const budgetSelect = form.querySelector('.startProject__nativeSelect');
      const budgetText = budgetSelect?.selectedOptions?.[0]?.textContent || '-';
      const subject = 'New request from Eten Tech website';
      const body = [
        `Name: ${formData.get('name') || '-'}`,
        `Email: ${formData.get('email') || '-'}`,
        `Budget: ${budgetText}`,
        `Help: ${help}`,
        '',
        `Project:`,
        formData.get('project') || '-'
      ].join('\n');

      if (status) {
        status.textContent = 'For local preview, your email client will open. On the published site, the request is sent directly to email.';
        status.classList.add('is-visible');
      }

      window.location.href = `mailto:etenprod@gmail.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
    });
  };

  const initPortfolioScrollReveal = () => {
    const projects = document.querySelectorAll('.portfolioProject, .portfolioCta');

    if (!projects.length) return;

    const revealProject = (project) => {
      project.classList.add('is-visible');
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          revealProject(entry.target);
          observer.unobserve(entry.target);
        }
      });
    }, {
      rootMargin: '0px 0px 38% 0px',
      threshold: 0.01
    });

    projects.forEach(project => observer.observe(project));

    requestAnimationFrame(() => {
      projects.forEach(project => {
        const rect = project.getBoundingClientRect();

        if (rect.top < window.innerHeight * 1.18) {
          revealProject(project);
          observer.unobserve(project);
        }
      });
    });
  };

  const initProjectDetailPage = () => {
    const page = document.querySelector('.projectPage');
    if (!page) return;

    const params = new URLSearchParams(window.location.search);
    const slug = projectCases[params.get('project')] ? params.get('project') : 'probouw';
    const project = projectCases[slug];
    let hasRendered = false;

    const setText = (selector, value) => {
      const element = document.querySelector(selector);
      if (element) element.textContent = value || '';
    };

    const renderList = (selector, items = []) => {
      const list = document.querySelector(selector);
      if (!list) return;
      list.innerHTML = items.map(item => `<li>${item}</li>`).join('');
    };

    const render = () => {
      document.title = `Eten Tech - ${project.title}`;
      page.style.setProperty('--case-accent', project.accent);

      setText('[data-case-category]', project.category);
      setText('[data-case-title]', project.title);
      setText('[data-case-intro]', project.intro);
      setText('[data-case-scope]', project.scope);
      setText('[data-case-result]', project.result);
      setText('[data-case-overview]', project.overview);
      renderList('[data-case-challenges]', project.challenges);
      renderList('[data-case-solutions]', project.solutions);

      const heroImage = document.querySelector('[data-case-hero-image]');
      if (heroImage) {
        heroImage.src = project.images[0];
        heroImage.alt = project.title;
      }

      const results = document.querySelector('[data-case-results]');
      if (results) {
        results.innerHTML = project.results.map((item, index) => (
          `<article><strong>${index === 0 ? project.result : `0${index + 1}`}</strong><span>${item}</span></article>`
        )).join('');
      }

      const gallery = document.querySelector('[data-case-gallery]');
      if (gallery) {
        const galleryItems = project.gallery || project.images.map(src => ({ type: 'wide', images: [src] }));

        gallery.innerHTML = galleryItems.map((item, index) => {
          const type = item.type || 'wide';
          const images = item.images || [];

          if (type.startsWith('scene-')) {
            return `<figure class="caseGallery__item caseGallery__item--scene caseGallery__item--${type}">
              ${images.map((src, imageIndex) => (
                `<div class="caseBrowser">
                  <div class="caseBrowser__top"><span></span><span></span><span></span></div>
                  <img alt="${project.title} screenshot ${index + 1}.${imageIndex + 1}" loading="lazy" src="${src}"/>
                </div>`
              )).join('')}
            </figure>`;
          }

          if (type === 'pair') {
            return `<figure class="caseGallery__item caseGallery__item--pair">
              ${images.map((src, imageIndex) => (
                `<div class="caseBrowser">
                  <div class="caseBrowser__top"><span></span><span></span><span></span></div>
                  <img alt="${project.title} screenshot ${index + 1}.${imageIndex + 1}" loading="lazy" src="${src}"/>
                </div>`
              )).join('')}
            </figure>`;
          }

          return `<figure class="caseGallery__item caseGallery__item--${type}">
            <div class="caseBrowser">
              <div class="caseBrowser__top"><span></span><span></span><span></span></div>
              <img alt="${project.title} screenshot ${index + 1}" loading="${index === 0 ? 'eager' : 'lazy'}" src="${images[0]}"/>
            </div>
          </figure>`;
        }).join('');

        if (hasRendered) {
          gallery.querySelectorAll('.caseGallery__item').forEach(item => item.classList.add('is-visible'));
        }
      }

      const related = document.querySelector('[data-case-related]');
      if (related) {
        const relatedProjects = Object.entries(projectCases)
          .filter(([key]) => key !== slug)
          .slice(0, 2);

        related.innerHTML = relatedProjects.map(([key, item]) => (
          `<a class="caseRelated__card" href="project.html?project=${key}">
            <img alt="${item.title}" src="${item.images[0]}" loading="lazy"/>
            <span>${item.category}</span>
            <strong>${item.title}</strong>
          </a>`
        )).join('');
      }

      hasRendered = true;
    };

    render();

    const blocks = document.querySelectorAll('.caseHero__copy, .caseHero__visual, .caseSection, .caseGallery__item, .caseResults, .caseRelated, .caseCta');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
          observer.unobserve(entry.target);
        }
      });
    }, {
      rootMargin: '0px 0px 18% 0px',
      threshold: 0.04
    });

    blocks.forEach(block => observer.observe(block));
  };



  initSmoothScroll();
  initAccordion();
  initRevealBlocks();
  initMobileMenu();
  initFeaturedProjects();
  initCustomSelects();
  initStartProjectForm();
  initPortfolioScrollReveal();
  initProjectDetailPage();
});
